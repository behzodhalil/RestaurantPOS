<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".view.detail.DetailConfirmFragment"
    >

    <ImageView
        android:id="@+id/menu_management_logo"
        android:layout_width="@dimen/dimen_400dp"
        android:layout_height="@dimen/dimen_400dp"
        android:layout_marginStart="-1dp"
        android:layout_marginTop="-100dp"
        android:contentDescription="@string/text_mooing_logo"
        android:src="@drawable/logo_mooing"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:viewBindingIgnore="true" />

    <include
        android:id="@+id/layout_detail_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dimen_100dp"
        app:layout_constraintTop_toTopOf="parent"
        layout="@layout/layout_detail_item"
        />
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_confirm"
        android:layout_width="@dimen/dimen_300dp"
        android:layout_height="@dimen/dimen_120dp"
        android:layout_marginStart="@dimen/dimen_350dp"
        android:layout_marginTop="@dimen/dimen_100dp"
        android:backgroundTint="@color/white"
        android:text="@string/text_detail_confirm"
        android:textColor="@color/teal_700"
        android:layout_marginBottom="@dimen/dimen_40dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/layout_detail_item"
        app:cornerRadius="@dimen/dimen_20dp"
        android:textSize="@dimen/font_size_40sp"
        android:textStyle="bold" />

</androidx.constraintlayout.widget.ConstraintLayout>